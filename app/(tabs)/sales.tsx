import { FlatList, Pressable, StyleSheet, Text, View } from "react-native";
import { router } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { sales } from "@/lib/estock-data";
import { useColors } from "@/hooks/use-colors";
export default function المبيعاتScreen() { const colors = useColors(); return <ScreenContainer className="px-5" containerClassName="bg-background"><View style={styles.header}><View><Text style={[styles.eyebrow, { color: colors.success }]}>الإيرادات</Text><Text style={[styles.title, { color: colors.foreground }]}>المبيعات</Text></View><View style={[styles.iconBox, { backgroundColor: "#DDF4EE" }]}><IconSymbol name="chart.bar.fill" size={22} color={colors.primary} /></View></View><View style={[styles.summary, { backgroundColor: colors.primary }]}><Text style={styles.summaryLabel}>إجمالي المبيعات الأخيرة</Text><Text style={styles.summaryValue}>{sales.reduce((s, x) => s + x.total, 0).toFixed(2)} جنيه</Text><Text style={styles.summaryMeta}>{sales.length} فواتير في البيانات الحالية</Text></View><Text style={[styles.section, { color: colors.foreground }]}>آخر الفواتير</Text><FlatList data={sales} keyExtractor={(item) => String(item.id)} contentContainerStyle={{ gap: 10, paddingBottom: 24 }} renderItem={({ item }) => <Pressable onPress={() => router.push(`/invoice/${item.id}` as never)} style={({ pressed }) => [styles.card, { backgroundColor: colors.surface, borderColor: colors.border }, pressed && { opacity: 0.7 }]}><View style={styles.row}><View style={[styles.invoiceIcon, { backgroundColor: "#EAF0F2" }]}><Text style={[styles.invoiceNo, { color: colors.primary }]}>#{item.id}</Text></View><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>فاتورة رقم {item.id}</Text><Text style={[styles.meta, { color: colors.muted }]}>{item.date} · {item.cashier}</Text></View><View style={{ alignItems: "flex-end" }}><Text style={[styles.total, { color: colors.foreground }]}>{item.total.toFixed(2)}</Text><Text style={[styles.meta, { color: colors.muted }]}>جنيه</Text></View></View></Pressable>} /></ScreenContainer>; }
const styles = StyleSheet.create({ header: { paddingTop: 20, paddingBottom: 18, flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, eyebrow: { fontSize: 11, fontWeight: "800", letterSpacing: 1.4 }, title: { fontSize: 30, fontWeight: "800", marginTop: 5 }, iconBox: { width: 44, height: 44, borderRadius: 15, alignItems: "center", justifyContent: "center" }, summary: { borderRadius: 20, padding: 20 }, summaryLabel: { color: "#BCEFE3", fontSize: 11, fontWeight: "800", letterSpacing: 1.2 }, summaryValue: { color: "#fff", fontSize: 29, fontWeight: "800", marginTop: 9 }, summaryMeta: { color: "#D9F3EE", fontSize: 12, marginTop: 7 }, section: { fontSize: 18, fontWeight: "800", marginTop: 25, marginBottom: 12 }, card: { borderWidth: 1, borderRadius: 18, padding: 15 }, row: { flexDirection: "row", alignItems: "center", gap: 12 }, invoiceIcon: { width: 42, height: 42, borderRadius: 14, alignItems: "center", justifyContent: "center" }, invoiceNo: { fontSize: 12, fontWeight: "800" }, cardTitle: { fontSize: 14, fontWeight: "800" }, meta: { fontSize: 11, marginTop: 4 }, total: { fontSize: 17, fontWeight: "800" } });
