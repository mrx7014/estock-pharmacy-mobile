import { FlatList, Pressable, StyleSheet, Text, View } from "react-native";
import { router } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { purchases } from "@/lib/estock-data";
import { useColors } from "@/hooks/use-colors";
export default function PurchasingScreen() { const colors = useColors(); return <ScreenContainer className="px-5" containerClassName="bg-background"><View style={styles.header}><View><Text style={[styles.eyebrow, { color: colors.success }]}>SUPPLY</Text><Text style={[styles.title, { color: colors.foreground }]}>Purchasing</Text></View><View style={[styles.iconBox, { backgroundColor: "#EAF0F2" }]}><IconSymbol name="cart.fill" size={22} color={colors.primary} /></View></View><View style={[styles.summary, { backgroundColor: colors.success }]}><Text style={styles.label}>PURCHASE SPEND</Text><Text style={styles.value}>{purchases.reduce((s, p) => s + p.total, 0).toFixed(2)} EGP</Text><Text style={styles.meta}>Supplier invoices in the loaded snapshot</Text></View><Text style={[styles.section, { color: colors.foreground }]}>Recent supplier invoices</Text><FlatList data={purchases} keyExtractor={(item) => String(item.id)} contentContainerStyle={{ gap: 10, paddingBottom: 24 }} renderItem={({ item }) => <Pressable onPress={() => router.push(`/invoice/${item.id}?kind=purchase` as never)} style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={styles.row}><View style={[styles.invoiceIcon, { backgroundColor: "#DDF4EE" }]}><IconSymbol name="cart.fill" size={18} color={colors.success} /></View><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>Supplier invoice #{item.invoiceNo}</Text><Text style={[styles.meta, { color: colors.muted }]}>Vendor {item.vendorId} · {item.date}</Text></View><Text style={[styles.total, { color: colors.foreground }]}>{item.total.toFixed(2)}</Text></View></Pressable>} /></ScreenContainer>; }
const styles = StyleSheet.create({ header: { paddingTop: 20, paddingBottom: 18, flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, eyebrow: { fontSize: 11, fontWeight: "800", letterSpacing: 1.4 }, title: { fontSize: 30, fontWeight: "800", marginTop: 5 }, iconBox: { width: 44, height: 44, borderRadius: 15, alignItems: "center", justifyContent: "center" }, summary: { borderRadius: 20, padding: 20 }, label: { color: "#E5FFFA", fontSize: 11, fontWeight: "800", letterSpacing: 1.2 }, value: { color: "#fff", fontSize: 29, fontWeight: "800", marginTop: 9 }, meta: { color: "#6E8289", fontSize: 11, marginTop: 5 }, section: { fontSize: 18, fontWeight: "800", marginTop: 25, marginBottom: 12 }, card: { borderWidth: 1, borderRadius: 18, padding: 15 }, row: { flexDirection: "row", alignItems: "center", gap: 12 }, invoiceIcon: { width: 42, height: 42, borderRadius: 14, alignItems: "center", justifyContent: "center" }, cardTitle: { fontSize: 14, fontWeight: "800" }, total: { fontSize: 15, fontWeight: "800" } });
