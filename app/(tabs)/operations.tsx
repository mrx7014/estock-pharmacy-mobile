import { Alert, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { router } from "expo-router";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { products, sourceStats, statusFor } from "@/lib/estock-data";
import { useColors } from "@/hooks/use-colors";

export default function التنبيهاتScreen() {
  const colors = useColors();
  const atRisk = products.filter((p) => statusFor(p) !== "healthy");
  return <ScreenContainer className="px-5" containerClassName="bg-background"><ScrollView contentContainerStyle={{ paddingBottom: 28 }}><Text style={[styles.eyebrow, { color: colors.success }]}>مركز المتابعة</Text><Text style={[styles.title, { color: colors.foreground }]}>التنبيهات</Text><Text style={[styles.subtitle, { color: colors.muted }]}>تابع الأصناف التي تحتاج إلى إجراء.</Text><Text style={[styles.section, { color: colors.foreground }]}>قائمة المتابعة</Text>{atRisk.map((product) => <Pressable key={product.id} onPress={() => router.push(`/product/${product.id}` as never)} style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={[styles.statusIcon, { backgroundColor: statusFor(product) === "out" ? "#FBE9E7" : "#FFF3D9" }]}><IconSymbol name="exclamationmark.triangle.fill" size={19} color={statusFor(product) === "out" ? colors.error : colors.warning} /></View><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>{product.englishName}</Text><Text style={[styles.meta, { color: colors.muted }]}>{statusFor(product) === "out" ? "نافد من المخزون" : "تشغيلة تحتاج إلى مراجعة"}</Text></View><IconSymbol name="chevron.right" size={18} color={colors.muted} /></Pressable>)}<Text style={[styles.section, { color: colors.foreground }]}>إجراءات المخزون</Text><Pressable onPress={() => Alert.alert("تعديل المخزون", "اختر صنفاً من شاشة المخزون لتسجيل تعديل الفرع.")} style={[styles.action, { backgroundColor: colors.primary }]}><IconSymbol name="plus" size={21} color="#fff" /><View><Text style={styles.actionTitle}>تعديل المخزون</Text><Text style={styles.actionMeta}>سجل تصحيح الكمية</Text></View></Pressable><Pressable onPress={() => Alert.alert("استلام بضاعة", "يمكنك تسجيل التوريد من خلال بيانات الصنف.")} style={[styles.action, { backgroundColor: colors.success }]}><IconSymbol name="cart.fill" size={21} color="#fff" /><View><Text style={styles.actionTitle}>استلام بضاعة</Text><Text style={styles.actionMeta}>أضف توريداً من المورد</Text></View></Pressable><Text style={[styles.section, { color: colors.foreground }]}>مصدر البيانات</Text><View style={[styles.importCard, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={styles.importHeader}><Text style={[styles.cardTitle, { color: colors.foreground }]}>النسخة الاحتياطية</Text><Text style={[styles.ready, { color: colors.success }]}>جاهزة</Text></View><Text style={[styles.importText, { color: colors.muted }]}>تحتوي الأرشفة المرفقة على {sourceStats.files} ملف CSV أساسياً. البيانات الحالية تعرض العلاقات الرئيسية، ويمكن تنفيذ استيراد كامل مع متابعة التقدم.</Text><View style={styles.stats}><Text style={[styles.stat, { color: colors.foreground }]}>{sourceStats.products.toLocaleString()} <Text style={{ color: colors.muted, fontSize: 11 }}>صنف</Text></Text><Text style={[styles.stat, { color: colors.foreground }]}>{sourceStats.salesLines.toLocaleString()} <Text style={{ color: colors.muted, fontSize: 11 }}>سطر بيع</Text></Text></View></View></ScrollView></ScreenContainer>;
}
const styles = StyleSheet.create({ eyebrow: { fontSize: 11, fontWeight: "800", letterSpacing: 1.4, paddingTop: 20 }, title: { fontSize: 30, fontWeight: "800", marginTop: 5 }, subtitle: { fontSize: 14, marginTop: 4 }, section: { fontSize: 18, fontWeight: "800", marginTop: 27, marginBottom: 12 }, card: { borderWidth: 1, borderRadius: 18, padding: 14, flexDirection: "row", alignItems: "center", gap: 11, marginBottom: 9 }, statusIcon: { width: 39, height: 39, borderRadius: 13, alignItems: "center", justifyContent: "center" }, cardTitle: { fontSize: 14, fontWeight: "800" }, meta: { fontSize: 11, marginTop: 4 }, action: { borderRadius: 17, padding: 16, flexDirection: "row", alignItems: "center", gap: 12, marginBottom: 9 }, actionTitle: { color: "#fff", fontSize: 14, fontWeight: "800" }, actionMeta: { color: "#E5FFFA", fontSize: 11, marginTop: 3 }, importCard: { borderWidth: 1, borderRadius: 18, padding: 16 }, importHeader: { flexDirection: "row", justifyContent: "space-between" }, ready: { fontSize: 11, fontWeight: "800" }, importText: { fontSize: 12, lineHeight: 18, marginTop: 11 }, stats: { flexDirection: "row", gap: 24, marginTop: 16 }, stat: { fontSize: 20, fontWeight: "800" } });
