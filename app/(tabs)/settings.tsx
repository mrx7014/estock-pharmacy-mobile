import { useState } from "react";
import { Alert, StyleSheet, Switch, Text, TextInput, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { branches } from "@/lib/estock-data";
import { useColors } from "@/hooks/use-colors";
export default function الإعداداتScreen() { const colors = useColors(); const [names, setNames] = useState(branches.map((b) => b.name)); const [compact, setCompact] = useState(false); return <ScreenContainer className="px-5" containerClassName="bg-background"><Text style={[styles.eyebrow, { color: colors.success }]}>التفضيلات</Text><Text style={[styles.title, { color: colors.foreground }]}>الإعدادات</Text><Text style={[styles.subtitle, { color: colors.muted }]}>اضبط بياناتك المحلية لتكون واضحة ومفيدة.</Text><Text style={[styles.section, { color: colors.foreground }]}>أسماء الفروع</Text><View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}>{branches.map((branch, i) => <View key={branch.id} style={styles.field}><Text style={[styles.label, { color: colors.muted }]}>الفرع {branch.id}</Text><TextInput value={names[i]} onChangeText={(text) => setNames((old) => old.map((n, index) => index === i ? text : n))} onEndEditing={() => Alert.alert("تم الحفظ محلياً", `${names[i]} هو اسم الفرع الحالي.`)} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /></View>)}</View><Text style={[styles.section, { color: colors.foreground }]}>العرض</Text><View style={[styles.rowCard, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={{ flex: 1 }}><Text style={[styles.cardTitle, { color: colors.foreground }]}>عرض أصغر للأصناف</Text><Text style={[styles.meta, { color: colors.muted }]}>إظهار عدد أكبر من الأصناف في شاشة المخزون.</Text></View><Switch value={compact} onValueChange={setCompact} trackColor={{ false: colors.border, true: colors.success }} /></View><View style={[styles.note, { backgroundColor: "#FFF5DF" }]}><IconSymbol name="exclamationmark.triangle.fill" size={19} color={colors.warning} /><Text style={[styles.noteText, { color: colors.foreground }]}>الأسماء العربية غير الموجودة في النسخة الأصلية تظل فارغة ويمكنك إدخالها وتعديلها يدوياً. لن يعرض التطبيق علامات استفهام غير مفهومة.</Text></View></ScreenContainer>; }
const styles = StyleSheet.create({ eyebrow: { fontSize: 11, fontWeight: "800", letterSpacing: 1.4, paddingTop: 20 }, title: { fontSize: 30, fontWeight: "800", marginTop: 5 }, subtitle: { fontSize: 14, marginTop: 4 }, section: { fontSize: 18, fontWeight: "800", marginTop: 28, marginBottom: 12 }, card: { borderWidth: 1, borderRadius: 18, padding: 15, gap: 14 }, field: { gap: 7 }, label: { fontSize: 11, fontWeight: "700" }, input: { height: 44, borderWidth: 1, borderRadius: 12, paddingHorizontal: 12, fontSize: 14 }, rowCard: { borderWidth: 1, borderRadius: 18, padding: 16, flexDirection: "row", alignItems: "center" }, cardTitle: { fontSize: 14, fontWeight: "800" }, meta: { fontSize: 11, marginTop: 4 }, note: { marginTop: 20, padding: 15, borderRadius: 16, flexDirection: "row", gap: 10, alignItems: "flex-start" }, noteText: { flex: 1, fontSize: 12, lineHeight: 18 } });
